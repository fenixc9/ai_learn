# 实例分组

## 1 实例分组
按照模型名称，版本，量化方式，上下文长度，GPU分组

## 2 池内路由
不能使用轮训。而是使用**最小成本路由**
- 预计 prefill token 数
- 当前 decode 压力
- 已占用 KV cache
- 排队长度
- 最近 TTFT / ITL
- prefix/KV cache 命中概率
成本越低，路由到的概率越高

## 3 过载保护
做 admission control，超预算直接拒绝、降级或排队

## 4. 弹性伸缩
按 token throughput、KV 占用、P95 TTFT，而不是只看 CPU/QPS

# 原因 
普通服务请求和请求之间差距较小，但是LLM请求100token和100k token完全不是一个概念，无脑轮询可能导致请求不均衡。


## 按照成本打分！
```
score =  
a * normalized_waiting_queue  
+ b * normalized_decode_load  
+ c * normalized_kv_usage  
+ d * normalized_ttft_p95  
- e * prefix_cache_hit_bonus  
- f * locality_bonus  
+ g * penalty_if_near_oom
```

# 粘性路由
多轮对话每轮都换 backend，KV cache 复用几乎归零。必须要有粘性路由